# Supabase Auth (required for sign up / sign in with email verification).
# From Supabase Dashboard → Project Settings → API: Project URL and anon/public key.
# PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
# PUBLIC_SUPABASE_ANON_KEY=eyJ... (or sb_publishable_...)

# Database: local SQLite path or Postgres URL (e.g. Supabase) for production/Vercel
# SQLite (default): ./data/sqlite.db
# Postgres (Supabase): postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# DATABASE_URL=

# Site owner email – only this user can access /admin (bookings, metrics)
# ADMIN_EMAIL=you@example.com

# Booking confirmation emails (Resend). Leave unset to skip sending (in-app confirmation still works).
# Get a key at https://resend.com – free tier allows 100 emails/day.
# RESEND_API_KEY=re_xxxx
# Sender address (must be a verified domain in Resend, or use onboarding@resend.dev for testing)
# RESEND_FROM=Skin Assessment <notifications@yourdomain.com>
